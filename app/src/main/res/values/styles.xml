<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimaryThinkPink</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDarkThinkPink</item>
        <item name="colorAccent">@color/colorPrimaryDarkThinkPink</item>
        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>
    </style>

</resources>
